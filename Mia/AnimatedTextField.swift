//
//  NSTextField.swift
//  Mia
//
//  Created by Byron Everson on 12/25/22.
//

import UIKit

class AnimatedTextField: UITextField {
    
    /*var characterArray: [Character] = []
    var typingSpeed = 0.1
    private var timer: Timer?
    
    func typeOn(_ string: String, _ completion: (() -> ())?) {
        characterArray.append(contentsOf: Array(string))
        if timer != nil { return }
        timer = Timer.scheduledTimer(withTimeInterval: typingSpeed, repeats: true) { (timer) in
            self.stringValue.append(self.characterArray[0])
            self.characterArray.removeFirst()
            if self.characterArray.isEmpty {
                timer.invalidate()
                self.timer = nil
                completion?()
            }
        }
    }*/

}
